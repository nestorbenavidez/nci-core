<%= form_with(model: calculation_range, local: true) do |form| %>
  <% if calculation_range.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calculation_range.errors.count, "error") %> prohibited this calculation_range from being saved:</h2>

      <ul>
      <% calculation_range.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="field">
    <%= form.label :insurance_program %>
    <%= form.text_field :insurance_program %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
