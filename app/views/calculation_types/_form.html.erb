<%= form_with(model: calculation_type, local: true) do |form| %>
  <% if calculation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calculation_type.errors.count, "error") %> prohibited this calculation_type from being saved:</h2>

      <ul>
      <% calculation_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :field_type_id %>
    <%= form.text_field :field_type_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
