<%= form_with(model: field_class, local: true) do |form| %>
  <% if field_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(field_class.errors.count, "error") %> prohibited this field_class from being saved:</h2>

      <ul>
      <% field_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
