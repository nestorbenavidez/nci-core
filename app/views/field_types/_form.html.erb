<%= form_with(model: field_type, local: true) do |form| %>
  <% if field_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(field_type.errors.count, "error") %> prohibited this field_type from being saved:</h2>

      <ul>
      <% field_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :insurance_program_id %>
    <%= form.text_field :insurance_program_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
