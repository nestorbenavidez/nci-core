<%= form_with(model: financial_institution, local: true) do |form| %>
  <% if financial_institution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(financial_institution.errors.count, "error") %> prohibited this financial_institution from being saved:</h2>

      <ul>
      <% financial_institution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :apr %>
    <%= form.text_field :apr %>
  </div>

  <div class="field">
    <%= form.label :insurance_program_id %>
    <%= form.text_field :insurance_program_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
